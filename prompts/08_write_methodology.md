# 研究方法与局限性 Prompt

为本次 AI 辅助调研生成"研究方法与局限性"章节。

## 调研主题

{{TOPIC}}

## 调研统计数据

- 搜索统计：{{SEARCH_STATS}}
- 通过评估的信源数：{{PASSED_SOURCES}}
- 被过滤的信源数：{{REJECTED_SOURCES}}
- 三角验证通过的数据点：{{VERIFIED_POINTS}}
- 存在冲突的数据点：{{CONFLICTING_POINTS}}
- 信源层级分布：{{TIER_DISTRIBUTION}}

## 质量门控降级说明

{{QUALITY_WARNINGS}}

## 要求

1. 使用 `<h2>` 标题
2. 说明搜索策略（四层搜索：背景、权威、时效、学术）
3. 说明信源筛选标准（五级分类 + CRAAP 框架）
4. 说明数据验证方法（三角验证）
5. 坦诚列出本次调研的局限性（至少 3 点），包括但不限于：
   - 搜索引擎覆盖范围的限制
   - 付费数据库/报告无法获取
   - AI 辅助分析可能存在的偏差
   - 时间窗口限制
   - 如果"质量门控降级说明"不为"无"，必须将其中每条降级问题如实列入局限性
6. 说明数据截止日期
7. 输出纯 HTML 片段

只输出纯 HTML 片段，不要包含 ```html 标记，不要输出任何其他内容。
